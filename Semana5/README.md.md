# API GraphQL - Tienda Orgánica

Proyecto backend desarrollado con Node.js, Express, Apollo Server y Mongoose (MongoDB).

## Requisitos previos
- Node.js instalado
- MongoDB corriendo localmente (`mongodb://localhost:27017`)

## Instalación y Ejecución

1. Instalar las dependencias:
   ```bash
   npm install